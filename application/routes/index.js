var express = require('express');
var router = express.Router();

// var controllers = require('../controllers/index');
// router.get('/', controllers.getHomePage);//ตรงนี้จะเรียกผ่านcontroller แทน
module.exports = router;
